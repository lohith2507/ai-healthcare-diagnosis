export default function Disclaimer() {
  return (
    <div className="disclaimer">
      <span className="disclaimer-icon">⚠️</span>
      <span>
        Educational machine-learning project — <strong>not a medical device</strong>. Predictions
        are statistical estimates from public datasets and must never be used for real diagnosis.
        Always consult a qualified clinician.
      </span>
    </div>
  );
}
